#!/usr/bin/env bash

# Pull autograder changes, then run full_autograder

cd /autograder/lemonade_grading

git config user.name 'Autograder'
git config user.email '<>'

git pull

cd /autograder/lemonade_gradingmy_autograder
.full_autograder.sh
